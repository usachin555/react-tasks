const TextComponent =({text,textColor})=>{
    // console.log(props)
    return (
        <div>
            <h4 style={{color:textColor}}>{text}</h4>
        </div>

    )
}
export default TextComponent